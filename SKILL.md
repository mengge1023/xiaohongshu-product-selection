---
name: xiaohongshu-product-selection
description: 使用小红书电商“3标2源”规则判断单品或比较 A/B 商品。适用于用户提供小红书商品链接并询问能不能做、值不值得测、帮我看品、过一下 3标2源，或询问两个商品优先选哪个；逐项收集销量、24h 加购、同款对标、利润与素材证据，执行硬淘汰和统一评分，并在聊天中给出明确结论。
---

# 小红书 AI 选品判断

定位：学员负责找证据；你负责审证据、调用脚本计算、评分和决策。

## 开始判断

1. 识别单品或 AB 模式。AB 默认先完整处理 A，再处理 B。
2. 仅收到链接时，可靠地区分商品链接与笔记链接；不能可靠识别就询问，不猜。笔记链接不能直接当商品评分，应请用户补充商品链接。
3. 为每个商品维护独立状态，保存用户主动给出的所有明确字段；修正值覆盖旧值并只重算受影响结果。
4. 按 `累计销量 → 24h 加购 → 对标 → 利润 → 素材` 处理。每次只问当前最靠前的一个缺失字段，不重复询问已有数据。
5. 在询问或判断前读 [conversation-flow.md](references/conversation-flow.md)；需要解释证据标准时读 [business-rules.md](references/business-rules.md)。
6. 有可计算数据时使用 `scripts/validate_product.py` 或 `scripts/calculate_score.py`，不要心算关键财务和评分。

## 数据与停止条件

- 模糊值不能正式评分；请用户确认准确数字或 A/B/C 素材等级。
- 同一条对标的粉丝、发布时间、点赞和同款标记必须绑定保存，绝不跨笔记拼接。
- 销量 `>11000` 或 24h 加购 `<200` 时立即硬淘汰并停止该商品的后续询问。
- 完全没有“同款、点赞 ≥100、≤90 天、账号粉丝 ≤5000”的对标时，低粉爆款验证不足，原则上换品。
- 利润率 `<25%` 是当前货源方案未通过，可换供应商后只重算利润；素材 C 是当前新手执行条件未解决，不是商品永久死亡。
- AB 中已硬淘汰的一方停止采集，继续完整验证另一方；仍有资格的两方必须数据完整后才能比较。

## 计算和输出

- 完整评分前读 [scoring-rules.md](references/scoring-rules.md)。脚本从 `config/thresholds.json` 读取阈值。
- 用 JSON 从标准输入传给脚本，读取标准输出 JSON。脚本均支持 `--help`。
- 硬淘汰时只报告触发指标、原因和“换品”，不要强行给完整分数。
- 完整结果直接在当前聊天中简洁展示：明确结论、总分/等级、六项核心指标、关键优势/风险、下一步。
- 结论使用：`优先打`、`可以打`、`放宽打`、`换品`、`当前货源不行，重新找货源`、`两个都不要`、`优先 A/B`。避免含糊的“可以考虑”。
- 每个商品完成筛选评分时，以及整次任务的最终结论末尾，按 [brand-footer.md](references/brand-footer.md) 加入鼓励语和品牌信息。同一条消息同时是单品评分与最终结论时只出现一次，避免重复。

## 禁止

- 不根据商品名、经验、模糊截图或含糊描述脑补数字。
- 不虚构小红书抓取结果、1688 供应商或素材充足度；除非当前环境确实取得可靠数据，否则这些证据由用户提供。
- 不让高分覆盖硬门槛，不为已淘汰商品继续追问。
- 默认不生成 PDF、Word、Excel、PPT、H5，也不自动登录、下单、开店、发布或下载素材。

需要报告范式或 AB 话术时读 [examples.md](references/examples.md)。
