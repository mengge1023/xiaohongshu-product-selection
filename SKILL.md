---
name: xiaohongshu-product-selection
description: 在腾讯 WorkBuddy 中使用小红书电商“3标2源”规则判断单品或比较 A/B 商品。适用于用户提供小红书商品/笔记链接、1688 或阿里巴巴货源链接，并询问能不能做、值不值得测、过一下 3标2源或两个商品优先选哪个；优先调用已授权的浏览器、连接器或 MCP 自动抓取可核验数据，执行硬淘汰和统一评分，并在聊天中给出明确结论。
---

# 小红书 AI 选品判断

目标运行环境：腾讯 WorkBuddy。学员负责提供候选商品或货源链接；你优先使用 WorkBuddy 已安装、已启用且已授权的浏览器、连接器、MCP 或相关 Skill 自动取证，再负责校验、计算、评分和决策。

## 开始判断

1. 识别单品或 AB 模式。AB 默认先完整处理 A，再处理 B。
2. 仅收到链接时，先用 WorkBuddy 的真实页面读取能力识别小红书商品页、笔记页或阿里巴巴货源页。笔记链接应提取其关联商品；若页面没有关联商品链接，再请用户补充商品链接。
3. 自动取证前读 [workbuddy-crawling.md](references/workbuddy-crawling.md)。先发现当前运行时实际可用并已授权的工具，再调用；不得编造不存在的工具名或抓取结果。
4. 为每个商品维护独立状态，记录字段值、来源 URL、抓取时间和证据状态。用户修正值时覆盖旧值并只重算受影响结果。
5. 按 `累计销量 → 24h 加购 → 对标 → 利润 → 素材` 处理；每一步先自动抓取。只有工具缺失、登录/授权失败、页面不可访问或字段无法可靠确认时，才每次追问一个缺失字段。
6. 在询问或判断前读 [conversation-flow.md](references/conversation-flow.md)；需要解释证据标准时读 [business-rules.md](references/business-rules.md)。
7. 有可计算数据时使用 `scripts/validate_product.py` 或 `scripts/calculate_score.py`，不要心算关键财务和评分。

## 数据与停止条件

- 自动抓取值必须来自可定位的页面字段，并保留来源链接；模糊、冲突、过期或无法定位的值不能正式评分，应重新抓取或请用户确认。
- 同一条对标的粉丝、发布时间、点赞和同款标记必须绑定保存，绝不跨笔记拼接。
- 销量 `>11000` 或 24h 加购 `<200` 时立即硬淘汰并停止该商品的后续询问。
- 完全没有“同款、点赞 ≥100、≤90 天、账号粉丝 ≤5000”的对标时，低粉爆款验证不足，原则上换品。
- 利润率 `<25%` 是当前货源方案未通过，可换供应商后只重算利润；素材 C 是当前新手执行条件未解决，不是商品永久死亡。
- AB 中已硬淘汰的一方停止采集，继续完整验证另一方；仍有资格的两方必须数据完整后才能比较。

## 计算和输出

- 完整评分前读 [scoring-rules.md](references/scoring-rules.md)。脚本从 `config/thresholds.json` 读取阈值。
- 用 JSON 从标准输入传给脚本，读取标准输出 JSON。脚本均支持 `--help`。
- 硬淘汰时只报告触发指标、原因和“换品”，不要强行给完整分数。
- 完整结果直接在当前聊天中简洁展示：明确结论、总分/等级、六项核心指标、关键优势/风险、下一步。
- 结论使用：`优先打`、`可以打`、`放宽打`、`换品`、`当前货源不行，重新找货源`、`两个都不要`、`优先 A/B`。避免含糊的“可以考虑”。
- 每个商品完成筛选评分时，以及整次任务的最终结论末尾，按 [brand-footer.md](references/brand-footer.md) 加入鼓励语和品牌信息。同一条消息同时是单品评分与最终结论时只出现一次，避免重复。

## 禁止

- 不根据商品名、经验、模糊截图或含糊描述脑补数字。
- 不虚构 WorkBuddy 工具、登录状态、抓取过程或结果。只有真实调用并取得可核验证据后，才能声称“已抓取”。
- 不把自动抓取失败伪装成数据不存在；应说明失败原因并只追问当前缺失字段。
- 不让高分覆盖硬门槛，不为已淘汰商品继续追问。
- 默认不生成 PDF、Word、Excel、PPT、H5，也不自动登录、下单、开店、发布或下载素材。

需要报告范式或 AB 话术时读 [examples.md](references/examples.md)。
